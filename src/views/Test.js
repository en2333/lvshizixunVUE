const a=()=>{
    var b = new Date(2010, 9, 6)
    // var c = new Date(b);
    b.setDate(b.getDate())
    // var d="1,3,5"
    // var f=[3,5,2]
    // d.split(',').forEach((item,index)=>{
    //     f[index]=parseInt(item)
    // })

    // var number = [1,2,3,4,5,6]
    console.log(b)
}
a()